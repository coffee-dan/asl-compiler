// Dalio, Brian A.
// dalioba
// 2019-11-11

{
  int a; int b;

  // Test of EXPONENTIATION associativity.

  // Strict right-to-left test.
  a = 1 ^ 2 ^ 3 ^ 4;

  // Right-to-left, but with override due to ( ).
  b = 5 ^ ( 6 ^ 7 ) ^ 8
}
