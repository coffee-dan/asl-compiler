// Dalio, Brian A.
// dalioba
// 2019-10-06

{
  // IF with multiple ELIFs and ELSE
  if ac then
    ad = 9 / 10
  elif ae then
    af = ag % ah
  elif ai then
    aj = ak ^ al
  else
    am = an + 1
  end if
}
